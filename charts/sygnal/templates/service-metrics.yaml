apiVersion: v1
kind: Service
metadata:
  name: metrics-{{ include "basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 80
      protocol: TCP
      targetPort: metrics
  selector: {{- include "matchLabels" . | nindent 4 }}
