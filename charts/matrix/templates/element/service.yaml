apiVersion: v1
kind: Service
metadata:
  name: {{ include "element.basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "element.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector: {{- include "element.matchLabels" . | nindent 4 }}
