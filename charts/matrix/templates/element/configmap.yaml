apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "element.basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "element.labels" . | nindent 4 }}
data:
  config.json: |
    {{- tpl ( .Values.element.config | toJson ) . | nindent 4 }}