apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ma1sd.basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "ma1sd.labels" . | nindent 4 }}
data:
  ma1sd.yaml: |
    {{- tpl ( .Values.ma1sd.config | toYaml ) . | nindent 4 }}